{
  "name": "@sleek/list",
  "version": "1.0.0",
  "main": "dist/sleek-list.js",
  "license": "MIT",
  "description": "A modern virtualized list written in pure javscript lean and sleek",
  "dependencies": {
    "esbuild": "^0.10.0"
  },
  "scripts": {
    "test": "jest",
    "build": "esbuild lib/index.mjs --format=iife --bundle --minify --sourcemap --outfile=dist/sleek-list.js",
    "build:watch": "esbuild lib/index.mjs --format=iife --bundle --minify --sourcemap --watch --outfile=dist/sleek-list.js"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.13.12",
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3"
  }
}
